# crawl-render-audit Reference Guide

Responsible for HTTP GET checks, robots.txt AI agent rule evaluation, sitemap discovery, bounded crawling, raw HTML AST extraction, and JS hydration delta calculations.
